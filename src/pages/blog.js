// src/pages/Blog.jsx
import { Box, Typography } from '@mui/material';

const Blog = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      Blog
    </Typography>
    <Typography variant="body1">
      Helpful resources and tips for families navigating autism support. (Coming soon)
    </Typography>
  </Box>
);

export default Blog;
